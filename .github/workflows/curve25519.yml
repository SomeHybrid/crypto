name: curve25519
on: [push, pull_request]

defaults:
  run:
    working-directory: ./curve25519

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: dtolnay/rust-toolchain@nightly
      - run: cargo test
